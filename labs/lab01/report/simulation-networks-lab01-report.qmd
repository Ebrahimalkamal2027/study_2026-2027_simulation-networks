---
## Author
author:
  name: Ибрахим Мохсейн Алькамаль
  degrees: Student (4 курс)
  orcid: ""
  email: 1032225432@rudn.ru
  affiliation:
    - name: Российский университет дружбы народов
      country: Российская Федерация
      postal-code: 117198
      city: Москва
      address: ул. Миклухо-Маклая, д. 6

## Title
title: "Отчёт по лабораторной работе №01"
subtitle: "Моделирование сетей передачи данных"
license: "CC BY"
---

# Цель работы

Изучить основные возможности среды Mininet для моделирования сетей передачи данных: создание простой сетевой топологии, настройка IP-адресов хостов, проверка связности между узлами и работа с графическим редактором MiniEdit.

# Выполнение лабораторной работы

В рабочем каталоге был создан каталог дисциплины, после чего репозиторий проекта `simulation-networks` был клонирован вместе с подключёнными подмодулями шаблонов отчёта и презентации. После завершения клонирования выполнен переход в каталог проекта ([рис. @fig-1]).

![Клонирование репозитория проекта с подключёнными подмодулями](image/1.png){#fig-1 width=70%}

С помощью команд `make help` и `make list` были просмотрены доступные цели Makefile и список учебных курсов. Затем был выбран курс `simulation-networks`, соответствующий дисциплине «Моделирование сетей передачи данных». После подготовки структуры проекта были проверены каталоги лабораторных работ и файлы отчёта и презентации для `lab01` ([рис. @fig-2]).

![Подготовка структуры проекта и проверка файлов лабораторной работы](image/2.png){#fig-2 width=70%}

В локальный репозиторий был добавлен удалённый репозиторий `gitverse`. Командой `git remote -v` проверена конфигурация: для проекта доступны удалённые репозитории `gitverse` и `origin` ([рис. @fig-3]).

![Настройка и проверка удалённых репозиториев Git](image/3.png){#fig-3 width=70%}

После запуска виртуальной машины Mininet был выполнен вход под пользователем `mininet`

```text
user: mininet
password: mininet
```
Командой `ifconfig` проверена конфигурация сетевых интерфейсов: интерфейс `eth0` активен и имеет IP-адрес `192.168.200.129`, также отображён локальный интерфейс `lo` ([рис. @fig-4]).

![Проверка сетевых интерфейсов виртуальной машины Mininet](image/4.png){#fig-4 width=70%}

Подключение к виртуальной машине Mininet было выполнено по протоколу SSH с перенаправлением X11 с помощью команды `ssh -Y mininet@192.168.200.129`. После ввода пароля соединение было успешно установлено ([рис. @fig-5]).

![Подключение к виртуальной машине Mininet по протоколу SSH](image/5.png){#fig-5 width=70%}

Для упрощения последующих подключений SSH-ключ был скопирован на виртуальную машину командой `ssh-copy-id mininet@192.168.200.129`. После этого повторное подключение с параметром `-Y` было выполнено без запроса пароля ([рис. @fig-6]).

![Настройка подключения к виртуальной машине по SSH-ключу](image/6.png){#fig-6 width=70%}

После подключения командой `ifconfig` была повторно проверена текущая сетевая конфигурация виртуальной машины. Интерфейс `eth0` имеет адрес `192.168.200.129`, а локальный интерфейс `lo` — адрес `127.0.0.1` ([рис. @fig-7]).

![Проверка текущей конфигурации сетевых интерфейсов](image/7.png){#fig-7 width=70%}

Для интерфейса `eth1` был запущен DHCP-клиент командой `sudo dhclient -v eth1`. В результате от DHCP-сервера получен и назначен адрес `192.168.100.128`. Повторный вывод `ifconfig` подтверждает наличие двух активных интерфейсов: `eth0` с адресом `192.168.200.129` и `eth1` с адресом `192.168.100.128` ([рис. @fig-8]).

![Получение IP-адреса для интерфейса eth1 по DHCP](image/8.png){#fig-8 width=70%}

Для дальнейшей настройки виртуальной машины был установлен файловый менеджер `mc` вместе с необходимыми зависимостями. Установка выполнена командой `sudo apt install mc` и завершилась успешно ([рис. @fig-9]).

![Установка файлового менеджера Midnight Commander в виртуальной машине](image/9.png){#fig-9 width=70%}

В конфигурационном файле `/etc/netplan/01-netcfg.yaml` была задана автоматическая настройка IPv4 по DHCP для сетевых интерфейсов `eth0` и `eth1` ([рис. @fig-10]).

![Настройка DHCP для сетевых интерфейсов eth0 и eth1](image/10.png){#fig-10 width=70%}

Существующий каталог Mininet был переименован в `mininet.orig`, после чего исходный код Mininet был повторно клонирован из репозитория. Командой `sudo make install` выполнена установка, а проверка `mn --version` показала установленную версию `2.3.1b4` ([рис. @fig-11]).

![Установка Mininet из исходного кода и проверка версии](image/11.png){#fig-11 width=70%}

В конфигурации терминала `xterm` был установлен параметр `xterm*faceSize: 12`, определяющий размер шрифта терминала ([рис. @fig-12]).

![Настройка размера шрифта терминала xterm](image/12.png){#fig-12 width=70%}

Для обеспечения запуска графических приложений с повышенными привилегиями было получено значение `MIT-MAGIC-COOKIE-1` командой `xauth list $DISPLAY`. Затем это значение было добавлено для пользователя `root` и проверено повторным вызовом `xauth list $DISPLAY` ([рис. @fig-13]).

![Настройка X11-аутентификации для пользователя root](image/13.png){#fig-13 width=70%}

Для проверки работы Mininet была запущена стандартная топология командой `sudo mn`. В результате созданы два хоста `h1`, `h2`, коммутатор `s1` и контроллер `c0`. Командой `help` был также просмотрен список доступных команд интерфейса Mininet ([рис. @fig-14]).

![Запуск стандартной топологии и просмотр команд Mininet](image/14.png){#fig-14 width=70%}

Командами `nodes` и `net` были проверены узлы и соединения созданной сети. Затем с помощью `ifconfig` определены параметры интерфейсов хостов: `h1-eth0` имеет IP-адрес `10.0.0.1`, а `h2-eth0` — `10.0.0.2` ([рис. @fig-15]).

![Проверка топологии и сетевых интерфейсов хостов Mininet](image/15.png){#fig-15 width=70%}

Связность между хостами была проверена командой `h1 ping 10.0.0.2`. Передано и получено 5 пакетов без потерь; значения RTT `min/avg/max/mdev` составили `0.048/0.369/1.467/0.552` мс. После проверки работа Mininet была завершена командой `exit` ([рис. @fig-16]).

![Проверка связности между хостами и завершение работы Mininet](image/16.png){#fig-16 width=70%}

Для графического построения сети был запущен редактор MiniEdit командой `sudo ~/mininet/mininet/examples/miniedit.py`. После запуска открылось рабочее окно редактора с пустой топологией ([рис. @fig-17]).

![Запуск графического редактора топологий MiniEdit](image/17.png){#fig-17 width=70%}

В MiniEdit была создана простая топология, состоящая из двух хостов `h1` и `h2`, соединённых через коммутатор `s1` ([рис. @fig-18]).

![Топология сети из двух хостов и одного коммутатора в MiniEdit](image/18.png){#fig-18 width=70%}

В свойствах хоста `h1` был задан IP-адрес `10.0.0.1/8` ([рис. @fig-19]).

![Настройка IP-адреса хоста h1 в MiniEdit](image/19.png){#fig-19 width=70%}

Для второго хоста `h2` аналогично был задан IP-адрес `10.0.0.2/8` ([рис. @fig-20]).

![Настройка IP-адреса хоста h2 в MiniEdit](image/20.png){#fig-20 width=70%}

После запуска созданной топологии были открыты терминалы обоих хостов. Командой `ifconfig` проверены назначенные адреса, после чего выполнен взаимный `ping` между `h1` и `h2`. В обоих направлениях пакеты переданы без потерь, что подтверждает связность между хостами ([рис. @fig-21]).

![Проверка IP-адресов и связности хостов созданной топологии](image/21.png){#fig-21 width=70%}

После предыдущего эксперимента вручную заданные IP-адреса хостов были удалены. Поля `IP Address` в настройках узлов `h1` и `h2` оставлены пустыми ([рис. @fig-22], [рис. @fig-23]).

![Удаление вручную заданного IP-адреса хоста h1](image/22.png){#fig-22 width=70%}

![Удаление вручную заданного IP-адреса хоста h2](image/23.png){#fig-23 width=70%}

Далее в настройках MiniEdit в поле `IP Base` была указана базовая сеть `15.0.0.0/8`. Данный параметр используется для автоматического назначения IP-адресов создаваемым хостам ([рис. @fig-24]).

![Настройка базовой IP-сети 15.0.0.0/8 в MiniEdit](image/24.png){#fig-24 width=70%}

После повторного запуска топологии были открыты терминалы хостов `h1` и `h2` и выполнена команда `ifconfig`. В результате хосту `h1` автоматически был назначен адрес `15.0.0.1`, а хосту `h2` — `15.0.0.2`, оба с маской сети `255.0.0.0` ([рис. @fig-25]).

Таким образом, параметр `IP Base` позволяет задавать адресное пространство топологии и автоматически назначать IP-адреса хостам без необходимости вручную указывать адрес каждого узла.

![Проверка автоматически назначенных IP-адресов хостов h1 и h2](image/25.png){#fig-25 width=75%}

Для сохранения созданной топологии был подготовлен рабочий каталог `~/work`. После сохранения в нём появился файл топологии `lab01.mn` ([рис. @fig-26]).

![Проверка сохранённого файла топологии lab01.mn в рабочем каталоге](image/26.png){#fig-26 width=70%}

Для повторного использования сохранённой топологии в MiniEdit был открыт каталог `/home/mininet/work` и выбран файл `lab01.mn` ([рис. @fig-27]).

![Открытие сохранённого файла топологии lab01.mn в MiniEdit](image/27.png){#fig-27 width=70%}

После загрузки файла `lab01.mn` в MiniEdit была восстановлена ранее созданная топология, содержащая хосты `h1`, `h2` и коммутатор `s1` с соответствующими соединениями ([рис. @fig-28]).

![Загруженная из файла lab01.mn топология сети в MiniEdit](image/28.png){#fig-28 width=70%}

# Заключение

В ходе лабораторной работы была подготовлена среда Mininet и создана топология из двух хостов и одного коммутатора. Были проверены параметры сетевых интерфейсов и связность между хостами с помощью `ping`. Также была создана аналогичная топология в MiniEdit, выполнена настройка IP-адресов вручную и через `IP Base`, а созданная топология сохранена и повторно загружена из файла `lab01.mn`.

# Список литературы {.unnumbered}

::: {#refs}
:::
